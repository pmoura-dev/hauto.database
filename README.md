# hauto.database
HAUTO database server repository.

## Services / Schemas

Services are defined in the `services.conf` file in the project root directory.
Each line defines a new schema, along with a user and a password.
```
<schemaname>:<username>:<password>
```
